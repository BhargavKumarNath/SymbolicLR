# SymboLR — Cloud-safe dependencies (no PyTorch, no CUDA, no Rust compiler)
# These are the ONLY dependencies needed for Streamlit Cloud deployment.
numpy>=1.24.0
scikit-learn>=1.3.0
scipy>=1.11.0
sympy>=1.12
streamlit>=1.28.0
altair>=5.0.0
pandas>=2.0.0
typer>=0.9.0
rich>=13.7.0
pyyaml>=6.0.1
matplotlib>=3.8.0